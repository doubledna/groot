---
# server startup listening address
web:
  address: "127.0.0.1:8080"

# transport protocol: http/https
transport:
  protocol: "http"

# whether to enable dev mode: dev/prod
log:
  level: dev

# redis
redis:
  address: 62.234.14.222
  port: 6379
  password: xj2023
  database: 0

# mysql
mysql:
  address: 62.234.14.222
  port: 3306
  username: root
  password: cgl135246
  database: groot

# notify
notify:
  larkWebhook: "xxxxxx"

# ansible
ansible:
  authenticate: privateKey  # privateKey/kerberos
  privateKeyFile: "/home/doubledna/.ssh/id_rsa"
  user: root
  becomeUser: root
  dataDir: "../../data/"    # store playbook repo directory
